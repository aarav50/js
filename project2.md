---
title: "JS Test"
format: html
jupyter: false
execute:
  engine: node
---
# 🧠 If-Else & Logical Operators Worksheet

### Name: ____________________   Date: ____________________

---

## 📘 Instructions

* Solve all questions using **if, else if, else**
* Use logical operators: **&& (AND), || (OR), ! (NOT)**
* Write code in Java / JavaScript / C++ (as instructed by teacher)

---

# 🟢 Section A: Basic (Easy)

### Q1. Even or Odd

Write a program to check if a number is even or odd.
---

### Q2. Positive / Negative / Zero

Check whether a number is:

* Positive
* Negative
* Zero

---

### Q3. Voting Eligibility

If age ≥ 18 → print `"Eligible"`
Else → `"Not Eligible"`

---

### Q4. Number Range

Check if a number lies between **10 and 100 (inclusive)**.

---

### Q5. Divisibility

Print `"Valid"` if a number is divisible by **2 OR 5**

---

# 🟡 Section B: Intermediate

### Q6. Pass or Fail

A student passes if:

* marks ≥ 40 AND attendance ≥ 75
  Else → `"Fail"`

---

### Q7. Discount System

Apply discount if:

* age < 18 OR age > 60
  Else → `"No Discount"`

---

### Q8. Login System

Check:

* username = `"admin"` AND password = `"1234"`
  → `"Login Successful"`
  Else → `"Invalid Credentials"`

---

### Q9. Out of Range

Print `"Out of Range"` if number is **NOT between 20 and 50**

---

### Q10. FizzBuzz Logic

* divisible by 3 AND 5 → `"FizzBuzz"`
* only 3 → `"Fizz"`
* only 5 → `"Buzz"`
* else → `"None"`

---

# 🟠 Section C: Advanced

### Q11. Grade System

* Marks ≥ 90 → A
* Marks ≥ 75 → B
* Marks ≥ 50 → C
* Else → Fail

---

### Q12. Special Number

A number is “special” if:

* divisible by 3 AND NOT divisible by 5
  OR
* divisible by 7

---

### Q13. Exam Eligibility

Eligible if:

* attendance ≥ 75 AND marks ≥ 40
  OR
* marks ≥ 90

Else → `"Not Eligible"`

---

### Q14. Largest of Three Numbers

Find the largest among three numbers.

---

### Q15. Performance Category

* `"Perfect"` → 50–100 AND divisible by 5
* `"Good"` → 50–100 but NOT divisible by 5
* `"Average"` → 20–49
* `"Poor"` → otherwise

---

# 🔴 Bonus Challenge

### Q16. Complex Condition

A number is valid if:

* NOT negative
  AND
* divisible by 2 OR 3
  BUT
* NOT divisible by 5

---

# ✍️ Reflection (Optional)

1. Difference between `&&` and `||`: __________________________
2. What does `!` operator do? __________________________
3. Can multiple `if` statements run together? Why? __________________________

---

# ✅ Teacher Notes (Optional)

* Encourage dry run with sample values
* Focus on condition formation, not syntax perfection
* Discuss edge cases (e.g., boundary values)

---

**End of Worksheet**
